# 项目级 AI 开发约定

- 中文是主要语言，除专业用语或必要词汇外，优先使用中文。
- 不为了省事采用不规范实现；代码、注释、配置和设计文档都应保持清晰、完整、可维护。
- 追求职责清晰、边界稳定和可验证的实现，不堆砌临时补丁。
- 所有代码注释以中文为主，复杂方法、关键约束、事务、并发和安全逻辑应提供足够详细的注释。

## 强制使用项目 Skill

处理本仓库的开发、修改、重构、诊断、审计、测试、配置、数据库迁移或架构设计任务时，必须先完整读取：

```text
skills/develop-base-springboot/SKILL.md
```

再按其中的 Reference 路由读取本次任务需要的文档。当前代码、POM、迁移文件和测试始终是最终事实来源；Skill 与实现不一致时，先核实变更，再同步更新 Skill。
