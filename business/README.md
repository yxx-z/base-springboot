# 用户端应用

用户端维护系统用户主体、多登录身份、用户端 RBAC、菜单和审计日志。

当前内置认证方式：

- 账号密码登录。
- 支付宝 OAuth 授权登录。

基础框架不提供支付、退款、关闭订单和支付回调接口。真实支付能力必须在具体项目的订单领域中实现订单归属、金额校验、支付单状态机、回调验签与幂等。

用户端数据库结构由 `db/migration/business` 下的 Flyway 脚本维护。
