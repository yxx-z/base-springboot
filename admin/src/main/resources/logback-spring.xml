<?xml version="1.0" encoding="UTF-8"?>
<configuration debug="false">
    <!-- 应用名与日志目录均从 Spring Environment 读取，避免把 logging.file.name 误作应用标识。 -->
    <springProperty scope="context" name="APP_NAME"
                    source="spring.application.name" defaultValue="admin"/>
    <springProperty scope="context" name="LOG_HOME"
                    source="logging.file.path" defaultValue="./logs"/>
    <springProperty scope="context" name="MAX_FILE_SIZE"
                    source="logging.logback.rollingpolicy.max-file-size" defaultValue="100MB"/>
    <springProperty scope="context" name="MAX_HISTORY"
                    source="logging.logback.rollingpolicy.max-history" defaultValue="30"/>
    <springProperty scope="context" name="TOTAL_SIZE_CAP"
                    source="logging.logback.rollingpolicy.total-size-cap" defaultValue="10GB"/>

    <!-- 开发环境使用彩色控制台日志，便于快速区分时间、级别、调用线程和日志来源。 -->
    <springProfile name="dev">
        <appender name="STDOUT_COLOR" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
                <charset>UTF-8</charset>
                <pattern>%red(%d{yyyy-MM-dd HH:mm:ss.SSS}) %green([%thread]) %highlight(%-5level) [traceId:%X{Trace-Id:--}] %cyan(%logger{50}) - %magenta(%msg) %n</pattern>
            </encoder>
        </appender>

        <root level="INFO">
            <appender-ref ref="STDOUT_COLOR"/>
        </root>
    </springProfile>

    <!-- 非开发环境共用无颜色控制台 appender，确保测试报告和生产采集日志保持纯文本。 -->
    <springProfile name="!dev">
        <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
                <charset>UTF-8</charset>
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [traceId:%X{Trace-Id:--}] %logger{50} - %msg%n</pattern>
            </encoder>
        </appender>
    </springProfile>

    <!-- 测试和一次性 bootstrap 任务使用纯文本控制台，避免测试报告混入 ANSI 颜色码。 -->
    <springProfile name="!dev &amp; !prod">
        <root level="INFO">
            <appender-ref ref="STDOUT"/>
        </root>
    </springProfile>

    <!-- 生产环境同时保留控制台和滚动文件，便于容器采集与传统主机部署。 -->
    <springProfile name="prod">
        <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${LOG_HOME}/${APP_NAME}.log</file>
            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <fileNamePattern>${LOG_HOME}/${APP_NAME}.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
                <maxFileSize>${MAX_FILE_SIZE}</maxFileSize>
                <maxHistory>${MAX_HISTORY}</maxHistory>
                <totalSizeCap>${TOTAL_SIZE_CAP}</totalSizeCap>
            </rollingPolicy>
            <!-- 文件日志不包含 ANSI 颜色码，确保日志采集平台能够稳定解析。 -->
            <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
                <charset>UTF-8</charset>
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [traceId:%X{Trace-Id:--}] %logger{50} - %msg%n</pattern>
            </encoder>
        </appender>

        <root level="INFO">
            <appender-ref ref="STDOUT"/>
            <appender-ref ref="FILE"/>
        </root>
    </springProfile>
</configuration>
